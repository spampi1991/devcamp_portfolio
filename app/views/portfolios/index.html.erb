<h1>Portfolios Items</h1>

<% @portfolios_items.each do |item| %>
  <p><%= item.title %></p>
  <p><%= item.subtitle %></p>
  <p><%= item.body %></p>
  <p><%= image_tag item.thumb_image %></p>
<% end %>
